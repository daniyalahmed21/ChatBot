{
    "name": "generative-ai",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "devDependencies": {
        "@types/bun": "latest",
        "concurrently": "^9.2.1",
        "husky": "^9.1.7",
        "lint-staged": "^16.2.3"
    },
    "scripts": {
        "dev": "bun run index.ts",
        "prettier": "prettier --write .",
        "prepare": "husky"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "workspaces": [
        "packages/*"
    ],
    "dependencies": {
        "@google/generative-ai": "^0.24.1",
        "prettier": "^3.6.2"
    }
}
